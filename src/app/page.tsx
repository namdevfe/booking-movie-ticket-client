const HomePage = () => {
  return (
    <main className='flex items-center justify-center h-screen'>
      <button className='w-fit h-10 flex items-center justify-center px-2 py-1 bg-secondary transition-colors duration-300 hover:bg-[var(--hover-bg-btn-secondary-cl)]'>Button</button>
    </main>
  )
}

export default HomePage
